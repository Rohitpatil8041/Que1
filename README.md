# Que1
#!/bin/bash

if [[ "$#" - e q1]] then Is -1 $1

elif [[^ prime prime S\#^ prime prime - e q 2]] ; then

if ["\$1"=" -s^ prime prime ]||["\$1"=" - - s * ize " ] wc -c $2

elif ["S1"="-p"]||["S1"=" - p * ermissions "] then ls -ld $2 |awk '{ print $1; }'

f["\$1"="o"]||["["\$1"="\ o wner "] then
